---
id: app.drey.Warp
runtime: org.gnome.Platform
runtime-version: "49"
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
command: warp
finish-args:
  # X11 and Wayland
  - --socket=fallback-x11
  - --socket=wayland
  - --share=ipc
  # OpenGL rendering
  - --device=dri
  # Share files over the network
  - --share=network
  # Save files in the Downloads directory
  - --filesystem=xdg-download
modules:
  - name: warp
    buildsystem: meson
    config-opts:
      - -Dprofile=default
      - -Dqr-code-scanning=enabled
    sources:
      - type: git
        url: https://gitlab.gnome.org/World/warp.git
        commit: b8eda0bc3b3104984542d3aa03f7881f77452611
      # generated via flatpak-builder-tools
      - cargo-sources.json
      - type: shell
        commands:
          - mkdir .cargo
          - cp cargo/config .cargo/
